spring:
  datasource:
    url: jdbc:h2:mem:dxvision;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

app:
  jwt:
    secret: test-secret-32-bytes-minimum-length!!
    expiration-ms: 3600000
